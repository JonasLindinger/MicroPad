# AI-assisted development — firmware, HA automation, config generator and docs were created with AI (LLM) help, reviewed and tested by the author. Provided as-is, without warranty; verify on your own hardware, don't use for safety-critical applications.
# The src-layout micropad package (src/micropad) must be installed so both the
# pytest suite and the helper scripts it shells out to can import it; the exact
# Step-6 clean-room recipe installs only this file, so the local package is
# installed editable from the project itself.
-e .
-r requirements.txt
mypy==2.3.1
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.7
types-PyYAML==6.0.12.20260906
types-requests==2.33.0.20260906
paho-mqtt==2.1.0
jsonschema==4.23.0
pytest-playwright==0.8.0
playwright==1.55.0
